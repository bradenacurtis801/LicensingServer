[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast, no external dependencies
    integration: requires database (uses in-memory SQLite)
    e2e: requires a running server at http://localhost:8999
